# 晨间简报

## 1. 基本信息
- 项目名称：ACM售后 / AC售后
- 汇报类型：工作日晨间简报
- 汇报日期：2026-07-11
- 当前状态：🟡 生产波动治理与接口契约收口中

## 2. 今晨重点 / 速读
- 一句话结论：ACM 9 个模块持续运行并完成 4 项针对性修复，当前主要风险集中在 FAPS 大批量任务 12 分钟超时和间歇性 DNS 波动；AC 侧下一步需冻结 P016 黄金场景与字段合同 v0.1。
- 今日关注：优先把 FAPS 分批、checkpoint、断点续跑和网络容错转成可验证动作，同时避免 API 超时后直接切 Playwright 造成重复写入。
- 今日目标：沉淀生产运行结论和 AC 接口协同缺口，形成下周可直接执行的 P0 清单。

## 3. 项目进展摘要
| 模块/事项 | 最新进展 | 状态 |
|:---|:---|:---:|
| ACM 生产运行 | VMA、SPP、SSP、SFL、Approval 总体正常；FAPS 存在全局超时，BMSP、Blacklist、Fleetlines 受网络波动影响 | 持续跟踪 |
| StationList 兼容 | 已适配 `.right-main-box` 页面结构，支持提取电站 ID、排除 ICP 备案号并处理多运营商查询 | ✅ 已修复 |
| 采购价处理 | 已取消列表页重复验证，改走详情页兜底，单次减少约 75 秒等待 | ✅ 已验证 |
| 广告下线范围 | 历史处理窗口已由 5 天扩大到 30 天，可覆盖更早积压记录 | ✅ 已修复 |
| AC 接口契约 | 目标链路继续收敛到规则引擎对接 OMP API，P016 黄金场景、字段合同、Mock 与落库回读尚待冻结 | 推进中 |

## 4. AC售后新增对话总结
| 主题 | 总结 |
|:---|:---|
| 新增方向背景 | AC 侧已从入口和桥接层方案讨论进一步进入业务场景与 OMP 接口共同验收阶段 |
| 关键结论 | 应先冻结 1 个 P016 黄金场景和字段合同 v0.1，由 OMP 提供同契约 Mock，Agent 并行调试 |
| 已完成动作 | API 优先、Playwright 受控兜底的方向保持不变，并补充了 API 超时后的幂等与状态查询约束 |
| 当前价值 | 黄金场景、字段合同和 Mock 可解除业务场景与接口互等，提前暴露规则和参数差异 |
| 后续关注 | 明确接口 owner、测试凭证、幂等键、错误码、落库回读及 `need_review` 边界 |

## 5. 风险与阻塞
| 风险 | 影响 | 当前应对 |
|:---|:---:|:---|
| FAPS 大批量任务触发 12 分钟全局超时 | 高 | 按机构分批，记录 checkpoint，支持从未完成机构断点续跑 |
| DNS 波动影响多个 ACM 模块 | 中 | 增加失败分类、指数退避、重试上限和网络恢复续跑 |
| AC 场景与 OMP 接口缺少共同验收合同 | 高 | 冻结 P016 黄金场景和字段合同 v0.1，并推动同契约 Mock |
| API 超时后直接切 Playwright 可能重复写入 | 中 | 先按幂等键或 requestId 查询最终状态，状态未知时转人工 |

## 6. 今日计划
- P0：整理 FAPS 分批、checkpoint 和断点续跑验证清单
- P0：冻结 P016 黄金场景与字段合同 v0.1 的责任人和目标节点
- P1：补齐 DNS、超时、业务失败与可安全重试的分类口径
- P1：明确 API 超时后的状态查询、人工兜底和禁止重复写入规则

## 7. 备注
> 本日报聚焦生产运行波动与接口契约收口，周报可直接提取“4 项修复 / FAPS 超时 / DNS 波动 / P016 黄金场景与字段合同”。
